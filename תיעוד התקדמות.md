# תיעוד התקדמות — רימייק "סוחר הים"

> קובץ חי. מתעדכן בכל שלב משמעותי. עדכון אחרון: 2026-07-07

---

## שלב נוכחי: שלבים 2–3 — קיבוע תצורה + GDDs (הושלמו; ממתין לביקורת המשתמש)

### שלב 2 — קיבוע תצורה (הושלם)

- `CLAUDE.md`: הוגדר הפרויקט (רימייק סוחר הים) ונקבע ה־Technology Stack — Godot 4.6, GDScript, ייצוא Windows EXE.
- `.claude/docs/technical-preferences.md`: מולא במלואו — רנדרר Compatibility (תמיכה במחשבים ישנים), עכבר כקלט ראשי, עברית RTL, מוסכמות שמות Godot, gdUnit4, ודפוסים אסורים (לוגיקה ב־UI, RNG לא מוזרק, ערכים קשיחים, סטייה מהמקור ללא ADR).
- הותקן DOSBox בסביבת העבודה — אימות אמפירי מול `K.com` זמין לשלב הבא.

### שלב 3 — GDDs (הושלם — 9 מסמכים + אינדקס)

נכתבו ב־`design/gdd/` לפי תבנית 8 הסעיפים של הסטודיו. כל נוסחה מהפיענוח מופיעה
במדויק; כל פער/הנחה מסומן ⚠️ עם מקור המילוי ותוכנית אימות:

| קובץ | מערכת | נקודות ⚠️ עיקריות |
|---|---|---|
| `systems-index.md` | אינדקס + מפת תלויות + מוסכמת rand(n) | — |
| `time-calendar.md` | 7 ימים, שעון, זמני מסע, דילוגים | סף לילה (יוצא/מסתיים אחרי 16:00); ימי דילוג בשביתה |
| `market-prices.md` | מחירים יומיים + מחירים חריגים | רוטינת המחירים היומית (מהאפיון); זהות base_* |
| `trading.md` | קניה/מכירה, מטען, ערך ספינה | תקרת כמות בעסקה |
| `banking.md` | הפקדה/משיכה | ריבית (הנחה: אין); הכללת בנק בניקוד ובתקרות נזק |
| `ship-damage-repair.md` | קיבולת, נזק, תיקון, רציף | יחס תיקון 1:1; רציף סגור 20% (מהאפיון) |
| `morning-events.md` | מנוע 30 הקודים, 7 משפחות | כמות "סוחר מוכר"; פיצול 21/22; פירוק תשלום expand |
| `sailing-engine.md` | 4 שכבות אירועי הפלגה | class בשער; הסתברות ערפל/שביתה; השלכה בעומס; ספינה נטושה |
| `pirates.md` | קרב/בריחה/פשרה, משמר | ענף ההפסד (מהאפיון); שער המזומן בפשרה; תמחור משמר |
| `scoring-endgame.md` | סיום, שיאים, שמירה | הרכב הניקוד (מזומן+בנק?); קריטריון "אין דרך להמשיך" |

- עודכן `design/registry/entities.yaml`: 3 נמלים, 3 סחורות (כולל ערכי הפשרה הקבועים), 3 נוסחאות חוצות־מערכות, 5 קבועים.

### שלב 4 — ארכיטקטורה (הושלם; ADRs ממתינים לאישור)

באישור המשתמש ("תמשיך לארכיטקטורה") נכתבו דרך הסקיל `/create-architecture` (מצב lean):

- **`docs/architecture/architecture.md` v1.0** — 5 שכבות (Presentation/Feature/Core/Foundation/Platform); ליבת לוגיקה טהורה `src/core/` ללא תלות במנוע; `GameFacade` כדלת יחידה בין UI ללוגיקה; דפוס **PendingDecision** לשיחות באמצע רזולוציה (פיראטים, מו"מ צוות, הצעות); זרימת נתונים, גבולות API, עקרונות, ו־10 שאלות פתוחות (QQ-01…QQ-10) הממופות לאימות DOSBox.
- **ADR-0001** — הפרדת core/UI + דפוס PendingDecision.
- **ADR-0002** — שירות RNG מוזרק ו־seedable (סמנטיקת `random(n)` של Pascal; סטייה מתועדת: לא משכפלים את ה־LCG של בורלנד ביט־לביט — ההתפלגויות זהות).
- **ADR-0003** — כל הערכים + דגלי הפערים ⚠️ ב־JSON חיצוני (`assets/data/`); סגירת פער = שינוי דאטה, לא קוד.
- **ADR-0004** — שמירות/שיאים/הגדרות כ־JSON ב־`user://`, כתיבה אטומית, מצב RNG בשמירה (מונע save-scumming).
- **ADR-0005** — רנדרר Compatibility (OpenGL) במקום ברירת המחדל D3D12 של 4.6 — תמיכה במחשבים ישנים; ‏EXE יחיד עם PCK מוטמע.
- ביקורת TD (שער TD-ARCHITECTURE): **APPROVE**. ‏LP-FEASIBILITY דולג (מצב lean).
- כל ה־ADRs במצב **Proposed** — נדרש אישור משתמש להעברה ל־Accepted (סטוריז שמפנים ל־Proposed נחסמים אוטומטית).

### שלב 5 — תשתית בדיקות + מנוע הליבה (הושלם — ממתין לריצת CI ראשונה)

באישור "המשך": ‏ADR-0001…0005 אושרו (Accepted); נוספו ואושרו ADR-0006 (עברית/RTL),
ADR-0007 (ניווט hub-and-overlay + DecisionRouter), ‏ADR-0008 (פרוטוקול אימות DOSBox).

**מנוע הליבה מומש במלואו** (`src/core/` — 15 קבצי GDScript, טהורים, ללא תלות בסצנות):

- תשתית: `types.gd`, `result.gd`, `rng_service.gd` (סמנטיקת random(n) של Pascal), `game_config.gd` (טעינה fail-fast של ה־JSON), `game_state.gd` (סריאליזציה מלאה), `pending_decision.gd`.
- מנועים: `price_engine.gd`, `trade_engine.gd`, `bank_engine.gd`, `ship_engine.gd`, `morning_event_engine.gd` (מנוע 30 הקודים המלא), `voyage_engine.gd` (4 השכבות), `pirate_engine.gd` (קרב/בריחה/פשרה), `score_engine.gd`, `game_engine.gd` (מתאם רצף היום + API הפקודות + PendingDecision).
- **תצורה**: `assets/data/*.json` — ‏7 קבצים עם כל ערכי המקור + דגלי QQ מתועדים.
- **בדיקות** (‏gdUnit4): ‏5 חבילות יחידה + חבילת אינטגרציה — נוסחאות בערכי קצה (FakeRng), התפלגויות סטטיסטיות (זרע קבוע), אינווריאנטים, משחק מלא דטרמיניסטי (golden seed), ‏save/load round-trip.
- ‏CI: ‏GitHub Actions עם gdUnit4-action על Godot 4.6, רץ על כל push.
- ‏`project.godot`: רנדרר Compatibility, ‏RTL, ‏640×360, ‏Nearest.

הערת סביבה: אין אפשרות להריץ Godot בסביבת הענן (מדיניות רשת) — האימות בוצע דרך CI.

### CI ירוק ✅ (ריצה #4, קומיט a82eb48)

**כל 58 הבדיקות עוברות על Godot 4.6** — ‏7 חבילות, 0 שגיאות, 0 כשלים.
ארבע איטרציות תיקון: (1) ‏`_close` חסרה ב־GameEngine; (2) מקטע `winds` חסר
ב־voyage.json; (3) בדיקת המשקלים רוקנה את המצב בין הגרלות (גניבות/דיג פסלו
משפחות בהדרגה); (4) טרנסקריפט golden-seed הכיל instance-ids; (5) הרשאת
`checks: write` לדוח ה־CI. הבדיקות הירוקות כוללות: התפלגות 30 הקודים,
הסתברויות קרב הפיראטים, נוסחת הנזק הכללי, משחק מלא דטרמיניסטי (golden seed),
‏save/load ביט־לביט.

### שלב 6 — אפיון UX (הושלם; ממתין לביקורת)

נכתבו ב־`design/ux/` על בסיס `אפיון מסכי משחק.docx` (מקור הכוונה), ה־GDDs,
צילומי המקור ותמונות ההשראה:

| מסמך | מכסה |
|---|---|
| `interaction-patterns.md` | ‏8 דפוסים: hotspot, כרטיס אירוע, דיאלוג החלטה, בורר כמות, לוח מחירים, Info Bar, אישור, מחסום מודאלי |
| `hud.md` | שורת מידע עליונה + פאנל מצב ימני + לוח מחירים תחתון (מסך F10 של המקור נבלע ב־HUD) |
| `port-hub.md` | הנמל הלחיץ — 7 hotspots, מיפוי מקשים 1–6 לתפריט המקור, מצבי ספינה/עומס/חסימה |
| `market.md` | קניה+מכירה (שתי וריאציות תפאורה), כרטיס עסקה עם עלות חיה |
| `voyage.md` | מפה→משמר→אזהרות→סצנת מסע חי→כרטיסי 9 תוצאות; resolve-then-present |
| `pirates.md` | קרב/בריחה/פשרה, דיאלוג פשרה עם נעילת רכיב הכסף, שרשראות ככרטיסים |
| `bank.md`, `repair-dock.md` | דלפקים/רציף כולל וריאנט "רציף סגור" ו"תקן עד סף ההפלגה" |
| `day-events.md` | רצף יום←מזג אוויר←אירוע; לוח מודעות; 5 דיאלוגי החלטה |
| `endgame-hiscores.md` | סיכום, הזנת שם, טבלת 10, משחק חדש |
| `title-settings-save.md` | פתיחה, הגדרות (טקסט גדול, הפחתת תנועה), שמירה/טעינה |
| `design/accessibility-requirements.md` | בסיס WCAG-AA מותאם: עכבר־בלבד מלא, מקלדת מלאה, אי־תלות בצבע, ללא לחץ זמן |

‏10 שאלות UX פתוחות (QQ-UX-01…10) מסומנות במסמכים — רובן להכרעת פלייטסט.

### השלב הבא

1. ‏`/ux-review` על המפרטים (או ביקורת שלך) ← מימוש שכבת foundation + מסכים (placeholder art) ← משחק שלם משוחק.
2. אימות DOSBox ל־QQ-01…QQ-04 (פרוטוקול לפי ADR-0008).
3. ‏art bible לפי תמונות `עיצוב/` ← החלפת placeholder ← אריזת EXE.

---

## היסטוריה: שלב 1 — לימוד ותכנון (הושלם ואושר)

### מה נעשה בשלב זה

- נסרק כל מבנה המאגר ומופו כל החומרים בתיקיית `רימיקס לסוחר הים/`.
- נקראו במלואם: `תוכנית.txt`, כל 7 קבצי `מנגנון הפעלה והגרלת סיכויים ואירועים/`, `אפיון מסכי משחק.docx` (חולץ מה־docx), תמונות העיצוב, וצילומי מסך מייצגים.
- זוהו קידודים: `התפלגות סיכויים.txt` היה בקידוד Windows-1255 (עברית DOS/חלונות ישן) — הומר ונקרא במלואו.
- זוהו סתירות בין מסמך האפיון הישן לבין פיענוח הבינארי (מפורט בהמשך).
- לא נכתב קוד ולא שונה שום קובץ מקורי.

---

## 1. מיפוי המאגר — סיווג קבצים

### א. תשתית סוכני הפיתוח (Claude Code Game Studios)
- `CLAUDE.md`, `.claude/` (סוכנים, סקילים, חוקים), `design/`, `docs/`, `production/`, `src/`, `tests/` — תבנית סטודיו ריקה, מוכנה לעבודה. המנוע במאגר כבר מכוון ל־**Godot 4.6** (`docs/engine-reference/godot/VERSION.md`).
- `CCGS Skill Testing Framework/` — תשתית בדיקות של התבנית עצמה, לא רלוונטי למשחק.

### ב. המשחק המקורי (חומרים בינאריים)
- `רימיקס לסוחר הים/socher1/K.com` — **הבינארי המקורי** (Turbo Pascal, DOS, ‏52KB). ניתן להרצה ב־DOSBox לאימות התנהגות.
- `socher1/HEBREW.COM`, `FONTHE.COM`, `KEYBHE.COM`, `Go.bat`, `Socher.bat` — דרייברים לעברית ב־DOS וקבצי הרצה.
- `socher1/קבצי SCR/‏` (17 קבצים) — מסכים מלאים (פתיחה, יום חדש, מזג אוויר, אירועי בוקר...).
- `socher1/קבצי WIN/‏` (26 קבצים) — חלונות/דיאלוגים (קניה, מכירה, בנק, פיראטים, סערה...).
- `socher1/קבצי SGN/‏` (14 קבצים) — שלטים (ימי השבוע, קניה/מכירה, שיאים...).
- `K.txt`, `K ישן.txt`, `SOCHER/K.txt`, `OFFER.txt`, `PIRATES.txt` — **דמפים הקסדצימליים** של הבינארים (לא קוד מקור קריא). `K.txt` זהה ל־`SOCHER/K.txt`; ‏`K ישן.txt` הוא גרסה שונה (ישנה יותר) של המשחק.
- תיקיות `תרגום/` בתוך SCR/SGN/WIN — **גם הן דמפים הקסדצימליים** של אותם קבצים, לא תרגום טקסטואלי. הטקסטים במשחק מאוחסנים כגרפיקה (ביטמפ CGA), לכן אין מהם חילוץ טקסט ישיר — הטקסטים זמינים דרך צילומי המסך והמסמכים.

### ג. פיענוח לוגיקה (סיכומי עבודה קודמת ב־ChatGPT — הנדסה לאחור של הבינארי)
- `מנגנון הפעלה.../סיכום מנוע אירועי בוקר.txt` — פיענוח מלא של מנוע אירועי הבוקר (הגרלת 1 מ־30, נוסחאות מחיר, expand, שביתת צוות, גניבה, סוחר, סירת דיג).
- `סיכום אירועי הפלגה מהודק.txt` (+ גרסה קודמת `סיכום אירועי הפלגה.txt`) — מבנה מנוע ההפלגה: שער סיכונים, סערה, שרטון, נזק, מטען עודף, ערפל/שביתה, טבלת אירועים רגילים random(20).
- `סיכום שודדי ים.txt` — מנגנון קרב/בריחה/פשרה מפוענח ברמת נוסחאות.
- `אירוע רוחות.txt` — אלגוריתם מלא של טעות ניווט (change) כולל טבלת הסטות וכלל +4 שעות.
- `תיאור כללי אירועי הפלגה.txt` — תיאור שיווקי/עזרה של הסכנות (כנראה מבוסס על מסך העזרה המקורי).

### ד. מסמכי אפיון
- `תוכנית.txt` — אפיון־על של המשחק: תמה, מטרה, מבנה יום, תפריטים, תרחישים, כללי סיום. **מסמך הבסיס.**
- `התפלגות סיכויים.txt` — אפיון הסתברויות מוקדם (כנראה נכתב מהזיכרון/תצפית לפני פיענוח הבינארי). **סותר חלקית את פיענוח הבינארי** — ראו סעיף "סתירות".
- `אפיון מסכי משחק.docx` — אפיון ראשוני של מסכי הרימייק: נמל אינטראקטיבי (שוק קניה, שוק מכירה, בנק, רציף תיקונים, לוח מודעות, מפה), שורת מידע עליונה, מסך הפלגה עם מצבים דינמיים (יום/לילה, ים רגוע/סוער, ספינה תקינה/פגועה), מסכי פיראטים, אזהרות, שיאים, הגדרות, שמירה.

### ה. חומרי עיצוב
- `עיצוב/` — 3 תמונות שנוצרו ב־Gemini: **פיקסל־ארט מודרני עשיר** (מבט מסיפון הספינה אל הים, לילה/ערפל/פיראטים, פנסים חמים, מיני־מפה, שורת סטטוס עליונה). זה כיוון הסגנון האמנותי הרצוי.
- `צילומי מסך/` — ‏~120 צילומי מסך של המשחק המקורי (CGA, ‏4 צבעים), מסודרים בתיקיות לפי נושא: מסך ראשי, קניה, מכירה, בנק, נסיעה (על כל אירועיה), שודדי ים (קרב/בריחה/פשרה), תיקון ספינה, אירועי ימים, מסכי פתיחה וסיום. **זהו תיעוד־הזהב של הטקסטים והמסכים המקוריים.**

### ו. קבצים שלא ניתן לקרוא ישירות
- כל קבצי `.SCR/.WIN/.SGN` והדמפים שלהם — גרפיקת ביטמפ, אין בהם טקסט לחילוץ. פוענחו כבר בעבודה הקודמת ברמת הלוגיקה.
- `K.com` / `K.txt` — קוד מכונה. הלוגיקה שופענחה נמצאת במסמכי המנגנון; מה שלא פוענח מסומן כפער (ראו בהמשך).

---

## 2. סיכום המשחק המקורי

משחק מסחר ימי בתורות, DOS, עברית, שנות ה־80/90 (Turbo Pascal, CGA):

- **מטרה**: לסיים 7 ימי עסקים עם מקסימום כסף. טבלת 10 שיאים.
- **זירה**: חיפה (ישראל), פורט־סעיד (מצרים), מרסין (תורכיה). סחורות: נחושת, זיתים, חיטה. מטבע: שקל.
- **התחלה**: ספינה בקיבולת 100 טון, 5,000 ש"ח.
- **יום משחק**: 08:00–20:00. הזמן חולף רק בהפלגה (ישראל↔מצרים 4ש', ישראל↔תורכיה 4ש', מצרים↔תורכיה 8ש').
- **מבנה יום**: הכרזת יום → מזג אוויר (ביום 1 תמיד נאה) → אירוע בוקר (מיום 2) → מסך ראשי עם תפריט: לקנות / למכור / לנסוע / בנק / לנוח עד למחרת / (לתקן את הספינה — רק כשיש נזק).
- **הפלגה**: הצעת ספינות משמר → אזהרות (מטען עודף, הפלגה לילית אחרי 16:00) → לכל היותר אירוע אחד בהפלגה.
- **סיום**: אחרי יום 7, או כשאין דרך להמשיך (אין כסף / ספינה עם נזק מעל 1,000 ש"ח שאין יכולת לתקן).

## 3. סיכום המנגנונים (לפי פיענוח הבינארי — המקור המוסמך)

### אירועי בוקר (מיום 2)
הגרלת קוד 1–30 שווה־סיכוי; פסילה אם "אותה משפחה כמו אתמול" או תנאי סף לא מתקיים → הגרלה מחדש מלאה:
| קודים | אירוע | משקל גולמי |
|---|---|---|
| 1–12 | שינוי מחיר (סחורה+כיוון קבועים בקוד, נמל מוגרל) | 12/30 |
| 13–18 | הצעת הגדלת ספינה (Delta=50/100; Cost=10×(2Δ+rand(Δ))) | 6/30 |
| 19–20 | שביתת צוות (רק ימים 2–5; מו"מ: הצעה > TotalWealth/rand(4..9)) | 2/30 |
| 21–22 | גניבה בלילה (סחורה: 0–80% בקפיצות 4%; כסף: ‎10×⌊cash/rand(40..79)⌋‎) | 2/30 |
| 23–26 | סוחר קונה ממך (סריקה קבועה נחושת→זיתים→חיטה, כל הכמות, מחיר=שוק±סטייה) | 4/30 |
| 27–28 | סוחר מוכר לך (סחורה מוגרלת, מחיר ~50%–150% שוק, כמות בכפולות 5) | 2/30 |
| 29–30 | התנגשות סירת דיג (נזק=‎10×⌊שווי כולל/rand(150..229)⌋‎) | 2/30 |

נוסחאות שינוי מחיר: נחושת ±(1100+100·rand(5)) | זיתים +200+50·rand(4) / ‎−150−50·rand(3)‎ | חיטה +20+5·rand(5) / ‎−15−5·rand(3)‎.

### מזג אוויר
יום 1 תמיד נאה. ימים 2–7: ‏rand(8)<3 ‏(37.5%) לסערה בנמל מוגרל מבין 3.

### מנוע הפלגה (אירוע אחד לכל היותר, לפי סדר בדיקות)
1. **שער סיכונים מיוחדים**: ‏N=2·class+1+⌈damage/400⌉‏; כניסה אם rand(N)>0 וגם rand(2)=1. בתוכו לפי סדר: רוחות → סערה → שרטון → נזק־מוחמר.
2. **מטען עודף**: מדרגות סיכוי לשקיעה לפי חריגה (עד 20 → 1/6; ‏21–40 → 1/4; ‏41–60 → 1/3; ‏61–70 → 1/2; ‏71+ → ודאי) → השלכת סחורה.
3. **ערפל/שביתה בנמל היעד** → חזרה לנמל המוצא (זמן ×2).
4. **אירועים רגילים**: ‏rand(20)+1 → ‏1–6 פיראטים (30%), ‏7 רוחות (5%), ‏9–10 ספינה נטושה (10%), אחרת הגעה בשלום (55%).

- **סערה** (רק אם מוצא/יעד בסערה): שווי מטען >10,000 → אובדן סחורה 0–80% בקפיצות 4%; אחרת נזק ‎5×⌊שווי/(rand(200..299))⌋‎, מינימום 75, ‏+שליש אם כבר יש נזק.
- **שרטון** (רק אחרי 16:00): פתיח שרטון + מנגנון הנזק הכללי (זהה לסערת־נזק).
- **רוחות/טעות ניווט**: יעד ישראל→תורכיה, כל יעד אחר→ישראל; ‏+4 שעות רק במסלול מצרים→ישראל שהוסט לתורכיה.

### שודדי ים
- **קרב**: כוח = מספר ספינות משמר. מגרילים rand(0..3): אם כוח>ההגרלה → ניצחון; אחרת אם כוח>0, ‏rand(0..5)=1 → ניצחון בכל זאת. (כוח 0 → הפסד ודאי; כוח 4+ → ניצחון ודאי).
- **ניצחון**: ‏rand(0..2)=1 → לכידת ספינה (+50 קיבולת); אחרת מטמון (5+rand(0..14))×100 = ‏500–1,900. נזק ב־25%: ‏(5+rand(0..14))×10 = ‏50–190.
- **בריחה**: מטען >81% קיבולת → כישלון מיידי. אחרת: escape=⌊קיבולת/30⌋−⌊מטען/30⌋+1 (מינ' 1), נכשל אם rand(escape)=0; ואז בדיקת נזק: dmg=⌊נזק/30⌋+1, מצליח רק אם rand(dmg)=0. כישלון → קרב.
- **פשרה**: ערכים קבועים לצורך המו"מ — נחושת 3,000, זיתים 500, חיטה 50. דרישה=(שווי מטען+כסף)/rand(4..9). מתקבל רק אם ההצעה > הדרישה (ממש).

### כללים נוספים
- נזק מעל 1,000 ש"ח → אסור להפליג עד תיקון שמוריד ל־1,000 או פחות.
- מחירים יומיים (מהאפיון): נחושת 2,500–3,500 (כפולות 100), זיתים 350–650 (כפולות 50), חיטה 35–70 (כפולות 5); ללא מחיר זהה לאותה סחורה בשני נמלים.
- ספינות משמר (מהאפיון): ‏2% מערך הספינה למשמרת, מינימום 75 ש"ח; ‏+50% בסערה, ‏+25% בהפלגה לילית (מצטבר).
- רציף תיקונים סגור ב־20% מהניסיונות (עד סוף היום).

---

## 4. סתירות בין המקורות (דורש הכרעת משתמש)

`התפלגות סיכויים.txt` (אפיון מוקדם) **סותר** את מסמכי פיענוח הבינארי (שמצטטים כתובות זיכרון ומשאבים, ולכן אמינים יותר כתיאור המשחק בפועל):

| נושא | התפלגות סיכויים (אפיון) | פיענוח הבינארי |
|---|---|---|
| חלוקת אירועי בוקר | ‏1/6 שווה לכל משפחה (גניבה+דיג 1/12) | ‏30 קודים: מחירים 12/30, expand ‏6/30, סוחר 6/30... |
| סיכוי סערה יומי | 50% | ‏37.5% ‏(rand(8)<3) |
| הגדלת ספינה | כפולות 25 עד הקיבולת; תשלום מורכב | ‏Δ=50/100; ‏Cost=10×(2Δ+rand(Δ)) |
| מחיר חריג | טווחים אבסולוטיים (נחושת 3,600–4,500) | בסיס±נוסחה (נחושת +1100..1500) |
| סוחר קונה | לפי המלאי הגדול ביותר; מחיר אבסולוטי | סריקה קבועה נחושת→זיתים→חיטה; מחיר=שוק±סטייה |
| קרב פיראטים | טבלת אחוזים לפי משמר (1%/51%/76%/88%/94%/97%) | מנגנון שתי הגרלות (‎~17%/37.5%/58%/79%/100%‎) |
| שלל ניצחון | ‏50/50; לכידה +25..קיבולת; מטמון 100..שליש שווי | ‏1/3 לכידה (+50 קבוע); מטמון 500–1,900 |
| בריחה | ‏30% בסיס פחות חצי אחוז עומס, נזק מחצית | מנגנון ⌊/30⌋ דו־שלבי |
| אירועי הפלגה | ‏1/3 כלום, 1/3 פיראטים, 1/3 אחר | ‏rand(20)‏: ‏30%/5%/10%/55% + שכבות מוקדמות |

**המלצה**: פיענוח הבינארי = מקור אמת; "התפלגות סיכויים" משמש רק למילוי פערים שהבינארי לא פוענח בהם.

## 5. פערים שנותרו פתוחים (מסומנים גם במסמכי הפיענוח)

1. מקור "ערכי הייחוס" (base) של שינויי המחירים בבוקר — האם מחירי אתמול או קבועים גלובליים.
2. נוסחת הכמות המדויקת ב"סוחר מוכר לך".
3. אלגוריתם ספינה נטושה (איזו סחורה, כמה) — קיים אלגוריתם ב"התפלגות סיכויים" שישמש כמילוי.
4. אלגוריתם ההשלכה במטען עודף (סוג וכמות) — קיים מילוי ב"התפלגות סיכויים".
5. משמעות class בשער הסיכונים; הטיית הפלגה לילית בטבלת האירועים הרגילים.
6. ‏integer_conv — קיטום או עיגול (נניח קיטום, כמקובל ב־Pascal ‏div).
7. מנגנון הבנק (ריבית? הגנה מגניבה?) והאם הניקוד הסופי כולל כסף בבנק.
8. התנהגות מחירים/מזג אוויר בימים שדולגו (שביתת צוות).
9. עלות תיקון — האם שווה לערך הנזק במלואו וניתן לתקן חלקית.

**דרך אימות זמינה**: `socher1/K.com` ניתן להרצה ב־DOSBox — אפשר לאמת אמפירית כל פער.

## 6. השוואה: מקור מול רימייק מבוקש

| היבט | מקורי | רימייק |
|---|---|---|
| לוגיקה, נוסחאות, הסתברויות | Turbo Pascal, DOS | **זהה 1:1** (data-driven, מתועד, ניתן לבדיקה) |
| קלט | מקלדת/לחצנים | **עכבר + GUI מודרני** (לפי אפיון המסכים) |
| גרפיקה | CGA ‏4 צבעים | פיקסל־ארט מודרני עשיר (בהשראת תמונות `עיצוב/`) עם שימור תחושת רטרו |
| מסכים | טקסט + חלונות | נמל אינטראקטיבי, מסך הפלגה חי (יום/לילה, ים, נזק), לוח מודעות |
| פלטפורמה | DOS | ‏Windows EXE, לא מקוון |
| שפה | עברית | עברית (RTL מלא) |
| חדש | — | מסך הגדרות, שמירת משחק (מופיעים באפיון המסכים — תוספת מול המקור) |

## 7. תוכנית עבודה מוצעת

| שלב | תוכן | תוצר |
|---|---|---|
| 1. מחקר ✅ | סריקה, קריאה, זיהוי פערים | מסמך זה |
| 2. הכרעות | אישור מנוע, מקור אמת, מדיניות פערים | עדכון CLAUDE.md + technical-preferences |
| 3. אפיון GDD | ‏GDD פר־מערכת (מסחר, מחירים, אירועי בוקר, הפלגה, פיראטים, בנק/תיקון, ניקוד) לפי תבנית 8 הסעיפים; אימות פערים ב־DOSBox לפי הצורך | ‏`design/gdd/` |
| 4. ארכיטקטורה | הפרדת מנוע-לוגיקה טהור (deterministic, seed-able) מ־UI; כל הערכים ב־config חיצוני | ‏ADRs + ‏architecture.md |
| 5. אב־טיפוס לוגיקה | מנוע המשחק המלא ללא גרפיקה + חבילת בדיקות יחידה לכל נוסחה | מנוע עובד ומכוסה בדיקות |
| 6. UX/UI | אפיון מסכים מפורט (ux-spec) לפי `אפיון מסכי משחק.docx` | ‏ux-specs |
| 7. אב־טיפוס משוחק | מסכים פונקציונליים מעל המנוע (placeholder art) | משחק שלם משוחק |
| 8. ארט ופוליש | פיקסל־ארט בסגנון `עיצוב/`, סאונד, אנימציות מצב | משחק מלוטש |
| 9. QA | בדיקות מול המקור, פלייטסטים, בדיקות קצה | דוחות |
| 10. אריזה | ייצוא Windows EXE + אייקון + בדיקת התקנה נקייה | ‏EXE סופי |

## 8. המלצת טכנולוגיה

**מומלץ: Godot 4.6 + GDScript** —
1. המאגר כבר מכוון ל־Godot 4.6 (סוכני godot-specialist, מסמכי engine-reference).
2. ייצוא Windows EXE נקי בקליק, ללא התקנות צד־שלישי, לא מקוון.
3. תמיכת RTL/עברית מובנית וטובה ב־Godot 4 (BiDi, TextServer).
4. מצוין ל־2D/פיקסל־ארט ול־UI עשיר; קל לתחזוקה ופתוח (MIT).
5. המנוע הלוגי ייכתב כמודול טהור נפרד — ניתן לבדיקה headless ב־CI.

חלופות שנשקלו: Unity (כבד ומורכב מהנדרש, רישוי), Electron/Web (‏EXE מנופח, אריזה מסורבלת), ‏.NET/WPF (‏UI משחקי ואנימציות — חלש).

## 9. החלטות שהתקבלו (אישור המשתמש, 2026-07-07)

| # | שאלה | הכרעה |
|---|---|---|
| 1 | מנוע פיתוח | **Godot 4.6 + GDScript** |
| 2 | מקור אמת בסתירות | **פיענוח הבינארי**; "התפלגות סיכויים" למילוי פערים בלבד |
| 3 | מילוי פערים | **אימות אמפירי ב־DOSBox מול K.com**; מה שלא ניתן לאימות — השלמה מ"התפלגות סיכויים" ותיעוד כהנחה |
| 4 | תוספות מודרניות | **שמירת משחק + מסך הגדרות מאושרים** (לא משנים את לוגיקת המשחק) |

שאלות שנותרו פתוחות (יוכרעו בשלב ה־GDD, חלקן באימות DOSBox):
- שפה: הנחת עבודה — עברית בלבד (קהל היעד); ניתן להוסיף אנגלית בהמשך.
- ניקוד סופי: מזומן בלבד או מזומן+בנק — לאימות ב־DOSBox.

## השלב הבא

ההכרעות המרכזיות התקבלו. ממתין לאישור המשתמש להתחיל בשלב 2–3:
קיבוע התצורה במאגר (CLAUDE.md, technical-preferences) וכתיבת ה־GDDs פר־מערכת.

---

## יומן שינויים

| תאריך | פעולה | קבצים |
|---|---|---|
| 2026-07-07 | שלב 1: סריקת מאגר, קריאת כל החומרים, זיהוי סתירות ופערים, תוכנית עבודה | נוצר `תיעוד התקדמות.md` (קובץ זה בלבד; שום קובץ מקורי לא שונה) |
| 2026-07-07 | הכרעות אושרו: Godot 4.6, מקור אמת = פיענוח בינארי, אימות DOSBox, אישור שמירה+הגדרות | `תיעוד התקדמות.md` |
| 2026-07-07 | שלב 2: קיבוע תצורת מנוע ופלטפורמה | `CLAUDE.md`, `.claude/docs/technical-preferences.md` |
| 2026-07-07 | שלב 3: נכתבו 9 GDDs + אינדקס מערכות + רישום ישויות | `design/gdd/*` (10 קבצים), `design/registry/entities.yaml` |
| 2026-07-07 | שלב 4: מסמך ארכיטקטורה ראשי + 5 ADRs יסודיים (Proposed); ‏TD sign-off: APPROVE | `docs/architecture/architecture.md`, `docs/architecture/ADR-000{1..5}-*.md` |
| 2026-07-07 | ‏ADR-0001..0005 → ‏Accepted; נכתבו ואושרו ADR-0006..0008 | `docs/architecture/ADR-*.md` |
| 2026-07-07 | שלב 5: מנוע ליבה מלא (15 קבצים), 7 קבצי config, 6 חבילות בדיקה, CI | `src/core/*`, `assets/data/*`, `tests/*`, `.github/workflows/tests.yml`, `project.godot` |
| 2026-07-07 | תיקון CI #1: ‏`_close` חסרה ב-GameEngine (כשל קומפילציה) | `src/core/game_engine.gd` |
