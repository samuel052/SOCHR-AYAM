# ביקורת עקביות התיעוד — 2026-07-20

## היקף

נבדקו כל המסמכים `00`–`10`, `דוח התקדמות.md`, דוחות האודיט, `src/core` ובדיקות המנוע.
כל מסמך הושווה לנקודת האמת `audit/00-master.md` ולמימוש, ולא לפי תאריך השינוי שלו.

## סתירות שנמצאו ותוקנו

- פרק 5: שערי העושר הוצגו כבטלי־אירוע ונוסחת מכירת הסוחר הייתה חלקית.
- פרק 7: קבלת כופר הוצגה כ־`>=` במקום `>` ומחיר המשמר איבד מכפיל 5.
- פרק 9: נשארו פרמטרים ישנים לירידות זיתים/חיטה, נוסחת כמות סוחר חלקית והתנגשות ללא ×10.
- מפת הדרכים: הכופר תואר כתשלום בסחורה בלבד במקום הצעת מזומן+שלוש סחורות.
- דוח ההתקדמות: תמציות ישנות של משכי מסלול, נזק, גניבת מזומן, ספינה נטושה ותוספת סיכון
  למשמר עודכנו לנוסחאות הסופיות.
- פרקים 1, 2, 5 ו־8: הוסרו "הצעד הבא" ו"פרטים שנותרו" שכבר הוכרעו.
- פרקים 3, 6, 7 ו־10: נוספה הבהרה שהסתברויות שבריות הן קיצור לתנאי `Random(n)` ושקיימת
  הטיית modulo זעירה במחולל TP3.

## שער אימות אוטומטי

הכלי `tools/verify-consistency.mjs` נכשל אם אחד מן הבאים משתנה או נסוג:

1. גודל או SHA-256 של `K.com`.
2. רציפות כיסוי כל `52,098` הבתים בשלוש מפות ה־CSV.
3. עובדות קנוניות במסמכים 3, 5, 6, 7, 9 ו־10.
4. חזרה של נוסחאות ישנות שכבר הופרכו.
5. קבועי המנוע הקריטיים: זוגות אירועים, ירידות מחיר, התנגשות, ספינה נטושה ו־LCG.
6. בדיקות המנוע או בדיקת התחביר של הממשק.

פקודת הרצה:

```powershell
& 'C:\Users\משפחת שוורץ\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' `
  'C:\socher\tools\verify-consistency.mjs'
```

תוצאה נוכחית:

```text
CONSISTENCY OK: 52098/52098 bytes, SHA-256 locked, 3 test files passed.
```

## משמעות

הבדיקה האוטומטית אינה "מוכיחה" כל משפט עברי באופן מתמטי. ההבטחה נוצרת משלוש שכבות:
מפת בתים מלאה, דוח אב קנוני אחד, ובדיקות/כללי עקביות שנכשלים בעת נסיגה. מסמכים היסטוריים
אינם עוד מקור הכרעה עצמאי; במקרה של ספק המקור המחייב הוא `00-master.md`, ואחריו קוד המקור
והבדיקות.
